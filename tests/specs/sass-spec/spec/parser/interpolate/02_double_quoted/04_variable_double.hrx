<===> input.scss
$input: "dquoted";
.result {
  output: #{#{$input}};
  output: #{"[#{$input}]"};
  output: #{"#{$input}"};
  output: #{'#{$input}'};
  output: #{"['#{$input}']"};
}

<===> output.css
.result {
  output: dquoted;
  output: [dquoted];
  output: dquoted;
  output: dquoted;
  output: ['dquoted'];
}
