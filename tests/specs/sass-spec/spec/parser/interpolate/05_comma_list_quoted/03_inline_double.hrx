<===> input.scss
.result {
  output: #{#{"alpha", 'beta'}};
  output: #{"[#{"alpha", 'beta'}]"};
  output: #{"#{"alpha", 'beta'}"};
  output: #{'#{"alpha", 'beta'}'};
  output: #{"['#{"alpha", 'beta'}']"};
}

<===> output.css
.result {
  output: alpha, beta;
  output: [alpha, beta];
  output: alpha, beta;
  output: alpha, beta;
  output: ['alpha, beta'];
}
